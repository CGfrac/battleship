const DOM = (() => {
    const _primaryGrid = document.getElementById('primary-grid');
    const _trackingGrid = document.getElementById('tracking-grid');

    const renderPrimaryGrid = board => {

    };

    const renderTrackingGrid = board => {

    };

    return {
        renderPrimaryGrid,
        renderTrackingGrid
    };
})();

export { DOM };
